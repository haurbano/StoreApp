package com.haurbano.remotedatasource.urls

object Urls {
    const val BASE_URL = "https://api.mercadolibre.com/"
    const val DEFAULT_SITE = "MLA"

    const val SEARCH = "/sites/{siteId}/search"
    const val GET_PRODUCT = "/items/{productId}"
}